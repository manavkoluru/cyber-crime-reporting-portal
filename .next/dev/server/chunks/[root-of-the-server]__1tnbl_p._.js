module.exports = [
"[externals]/http [external] (http, cjs)", ((__turbopack_context__, module, exports) => {

var mod = __turbopack_context__.x("http", () => require("http"));

module.exports = mod;
}),
"[externals]/https [external] (https, cjs)", ((__turbopack_context__, module, exports) => {

var mod = __turbopack_context__.x("https", () => require("https"));

module.exports = mod;
}),
"[externals]/next/dist/compiled/@opentelemetry/api [external] (next/dist/compiled/@opentelemetry/api, cjs)", ((__turbopack_context__, module, exports) => {

var mod = __turbopack_context__.x("next/dist/compiled/@opentelemetry/api", () => require("next/dist/compiled/@opentelemetry/api"));

module.exports = mod;
}),
"[externals]/next/dist/compiled/next-server/app-page-turbo.runtime.dev.js [external] (next/dist/compiled/next-server/app-page-turbo.runtime.dev.js, cjs)", ((__turbopack_context__, module, exports) => {

var mod = __turbopack_context__.x("next/dist/compiled/next-server/app-page-turbo.runtime.dev.js", () => require("next/dist/compiled/next-server/app-page-turbo.runtime.dev.js"));

module.exports = mod;
}),
"[externals]/next/dist/compiled/next-server/app-route-turbo.runtime.dev.js [external] (next/dist/compiled/next-server/app-route-turbo.runtime.dev.js, cjs)", ((__turbopack_context__, module, exports) => {

var mod = __turbopack_context__.x("next/dist/compiled/next-server/app-route-turbo.runtime.dev.js", () => require("next/dist/compiled/next-server/app-route-turbo.runtime.dev.js"));

module.exports = mod;
}),
"[externals]/next/dist/server/app-render/action-async-storage.external.js [external] (next/dist/server/app-render/action-async-storage.external.js, cjs)", ((__turbopack_context__, module, exports) => {

var mod = __turbopack_context__.x("next/dist/server/app-render/action-async-storage.external.js", () => require("next/dist/server/app-render/action-async-storage.external.js"));

module.exports = mod;
}),
"[externals]/next/dist/server/app-render/after-task-async-storage.external.js [external] (next/dist/server/app-render/after-task-async-storage.external.js, cjs)", ((__turbopack_context__, module, exports) => {

var mod = __turbopack_context__.x("next/dist/server/app-render/after-task-async-storage.external.js", () => require("next/dist/server/app-render/after-task-async-storage.external.js"));

module.exports = mod;
}),
"[externals]/next/dist/server/app-render/work-async-storage.external.js [external] (next/dist/server/app-render/work-async-storage.external.js, cjs)", ((__turbopack_context__, module, exports) => {

var mod = __turbopack_context__.x("next/dist/server/app-render/work-async-storage.external.js", () => require("next/dist/server/app-render/work-async-storage.external.js"));

module.exports = mod;
}),
"[externals]/next/dist/server/app-render/work-unit-async-storage.external.js [external] (next/dist/server/app-render/work-unit-async-storage.external.js, cjs)", ((__turbopack_context__, module, exports) => {

var mod = __turbopack_context__.x("next/dist/server/app-render/work-unit-async-storage.external.js", () => require("next/dist/server/app-render/work-unit-async-storage.external.js"));

module.exports = mod;
}),
"[externals]/next/dist/server/runtime-reacts.external.js [external] (next/dist/server/runtime-reacts.external.js, cjs)", ((__turbopack_context__, module, exports) => {

var mod = __turbopack_context__.x("next/dist/server/runtime-reacts.external.js", () => require("next/dist/server/runtime-reacts.external.js"));

module.exports = mod;
}),
"[externals]/next/dist/shared/lib/no-fallback-error.external.js [external] (next/dist/shared/lib/no-fallback-error.external.js, cjs)", ((__turbopack_context__, module, exports) => {

var mod = __turbopack_context__.x("next/dist/shared/lib/no-fallback-error.external.js", () => require("next/dist/shared/lib/no-fallback-error.external.js"));

module.exports = mod;
}),
"[externals]/node:fs [external] (node:fs, cjs)", ((__turbopack_context__, module, exports) => {

var mod = __turbopack_context__.x("node:fs", () => require("node:fs"));

module.exports = mod;
}),
"[externals]/node:stream [external] (node:stream, cjs)", ((__turbopack_context__, module, exports) => {

var mod = __turbopack_context__.x("node:stream", () => require("node:stream"));

module.exports = mod;
}),
"[externals]/node:stream/web [external] (node:stream/web, cjs)", ((__turbopack_context__, module, exports) => {

var mod = __turbopack_context__.x("node:stream/web", () => require("node:stream/web"));

module.exports = mod;
}),
"[externals]/punycode [external] (punycode, cjs)", ((__turbopack_context__, module, exports) => {

var mod = __turbopack_context__.x("punycode", () => require("punycode"));

module.exports = mod;
}),
"[externals]/stream [external] (stream, cjs)", ((__turbopack_context__, module, exports) => {

var mod = __turbopack_context__.x("stream", () => require("stream"));

module.exports = mod;
}),
"[externals]/url [external] (url, cjs)", ((__turbopack_context__, module, exports) => {

var mod = __turbopack_context__.x("url", () => require("url"));

module.exports = mod;
}),
"[externals]/util [external] (util, cjs)", ((__turbopack_context__, module, exports) => {

var mod = __turbopack_context__.x("util", () => require("util"));

module.exports = mod;
}),
"[externals]/zlib [external] (zlib, cjs)", ((__turbopack_context__, module, exports) => {

var mod = __turbopack_context__.x("zlib", () => require("zlib"));

module.exports = mod;
}),
"[project]/app/api/chat/route.ts [app-route] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "GET",
    ()=>GET,
    "POST",
    ()=>POST,
    "maxDuration",
    ()=>maxDuration
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$next$2f$server$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/next/server.js [app-route] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$next$2f$headers$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/next/headers.js [app-route] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$openai$2f$index$2e$mjs__$5b$app$2d$route$5d$__$28$ecmascript$29$__$3c$locals$3e$__ = __turbopack_context__.i("[project]/node_modules/openai/index.mjs [app-route] (ecmascript) <locals>");
var __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$agents$2f$ida$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/lib/agents/ida.ts [app-route] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$agents$2f$router$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/lib/agents/router.ts [app-route] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$agents$2f$fileComplaint$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/lib/agents/fileComplaint.ts [app-route] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$agents$2f$retrieval$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/lib/agents/retrieval.ts [app-route] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$agents$2f$fallback$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/lib/agents/fallback.ts [app-route] (ecmascript)");
;
;
;
;
;
;
;
;
const maxDuration = 60 // Vercel function timeout (seconds)
;
const openai = new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$openai$2f$index$2e$mjs__$5b$app$2d$route$5d$__$28$ecmascript$29$__$3c$locals$3e$__["default"]({
    apiKey: process.env.OPENAI_API_KEY
});
async function POST(req) {
    try {
        const formData = await req.formData();
        const message = formData.get('message') || '';
        const sessionId = formData.get('sessionId') || '';
        const history = JSON.parse(formData.get('history') || '[]');
        const file = formData.get('file');
        // Extract phone number from OTP session if user logged in via mobile
        let phoneFromSession = null;
        try {
            const cookieStore = await (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$next$2f$headers$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["cookies"])();
            const sessionCookie = cookieStore.get('session')?.value;
            if (sessionCookie) {
                const session = JSON.parse(sessionCookie);
                phoneFromSession = session.phone || null;
                if (phoneFromSession) {
                    console.log(`[Chat API] User logged in via OTP, phone available: +91${phoneFromSession}`);
                }
            }
        } catch (err) {
        // Session cookie may not exist, continue without it
        }
        // — Process uploaded file
        let fileBase64 = null;
        let fileMediaType = null;
        let fileDescription = '';
        if (file && file.size > 0) {
            const bytes = await file.arrayBuffer();
            const buffer = Buffer.from(bytes);
            fileBase64 = buffer.toString('base64');
            fileMediaType = file.type;
            if (file.type.startsWith('image/')) {
                fileDescription = `User uploaded an image named "${file.name}". Analyze it carefully for transaction details.`;
            } else if (file.type === 'application/pdf') {
                fileDescription = `User uploaded a PDF named "${file.name}". Extract any transaction/payment information visible.`;
            }
        }
        // — Extract previously accumulated state from chat history
        let accumulatedExtracted = {};
        for (const msg of history.slice().reverse()){
            if (msg.role === 'assistant') {
                // Try to extract previously identified fields from bot responses
                if (msg.content.includes('Transaction ID') || msg.content.includes('UTR') || msg.content.includes('₹')) {
                    // Parse bot's previous message for data (this helps maintain context)
                    const utrMatch = msg.content.match(/(?:UTR|Transaction ID|URN)[\s:]*([A-Z0-9]{10,})/i);
                    if (utrMatch && !accumulatedExtracted.utr_or_transaction_id) {
                        accumulatedExtracted.utr_or_transaction_id = utrMatch[1];
                    }
                    const amountMatch = msg.content.match(/₹([\d,]+)/);
                    if (amountMatch && !accumulatedExtracted.amount_stolen) {
                        accumulatedExtracted.amount_stolen = amountMatch[1].replace(/,/g, '');
                    }
                    const vpaMatch = msg.content.match(/(?:to|VPA|account)[\s:]*([a-z0-9.]+@[a-z]+)/i);
                    if (vpaMatch && !accumulatedExtracted.destination_vpa_or_account) {
                        accumulatedExtracted.destination_vpa_or_account = vpaMatch[1];
                    }
                }
                break; // Only check most recent assistant message
            }
        }
        // — Step 1: Intent Discovery Agent (always first)
        const idaResult = await (0, __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$agents$2f$ida$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["runIDA"])(openai, message || fileDescription, history, fileBase64, fileMediaType, accumulatedExtracted && Object.keys(accumulatedExtracted).length > 0 ? accumulatedExtracted : undefined);
        // — Step 2: Route
        const route = await (0, __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$agents$2f$router$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["runRouter"])(idaResult);
        // — Step 3: Run action agent
        let agentResponse = {
            message: '',
            metadata: {}
        };
        switch(route.route_to){
            case 'FILE_COMPLAINT_AGENT':
                // Pass extracted data to complaint agent to maintain context and pre-fill phone from session
                agentResponse = await (0, __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$agents$2f$fileComplaint$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["runFileComplaint"])(openai, message, history, idaResult, route, idaResult.extracted?.user_location || undefined, fileBase64, fileMediaType, idaResult.extracted, phoneFromSession);
                break;
            case 'RETRIEVAL_AGENT':
                // Pass phone from session so user doesn't need to re-enter it
                agentResponse = await (0, __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$agents$2f$retrieval$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["runRetrieval"])(openai, message, history, idaResult, phoneFromSession);
                break;
            case 'FALLBACK_AGENT':
                agentResponse = await (0, __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$agents$2f$fallback$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["runFallback"])(openai, message, history, idaResult);
                break;
            default:
                // IDA handled it – surface the conversational reply
                agentResponse = {
                    message: idaResult.conversationalReply || "I'm here to help. Can you tell me more?",
                    metadata: {
                        agent: 'Intent Discovery',
                        priority: 'NORMAL'
                    }
                };
        }
        return __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$next$2f$server$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["NextResponse"].json({
            message: agentResponse.message,
            metadata: {
                ...agentResponse.metadata,
                goldenHour: idaResult.extracted?.golden_hour_active === true,
                route: route.route_to
            }
        });
    } catch (error) {
        console.error('[CCRP] Chat API error:', error);
        return __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$next$2f$server$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["NextResponse"].json({
            message: 'I apologize – I ran into a technical issue. Please try again, or call **1930** (National Cyber Helpline, available 24x7 and free).',
            metadata: {}
        }, {
            status: 500
        });
    }
}
async function GET() {
    return __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$next$2f$server$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["NextResponse"].json({
        message: 'Cyber Crime Reporting Portal Chat API is running',
        timestamp: new Date().toISOString()
    });
}
}),
"[project]/lib/agents/fallback.ts [app-route] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "runFallback",
    ()=>runFallback
]);
const FALLBACK_SYSTEM_PROMPT = `You are the Fallback Agent of the Cyber Crime Reporting Portal.
You are triggered when other agents cannot handle the request – when intent is unclear, information is insufficient, or the user seems lost or confused.

## Your Job
1. Acknowledge the user empathetically – they may be stressed or scared.
2. Present clear, simple options in their language (Hindi/English/mix):

"I'm here to help. Here's what I can do for you:

1️⃣ 🚨 **Report a cyber fraud** – UPI scam, OTP theft, phishing, bank fraud
2️⃣ ☑️ **Check complaint status** – Track a complaint you already filed
3️⃣ 🛒 **Online shopping dispute** – Defective product, non-delivery, refund issues
4️⃣ 📞 **Talk to a human now** – Call **1930** (National Cyber Helpline, 24x7, free)"

3. Ask ONE simple question to understand what they need and route them correctly.

## CRITICAL – Mental Health Check
If the user expresses extreme distress (keywords: "lost everything", "ruined", "don't know what to do", "finished", "want to die", "suicidal", "end my life"):
IMMEDIATELY respond with empathy AND surface:
"I hear you – this is deeply distressing and not your fault. Please also reach out to:
☎️ **iCall**: 9152987821 (free counseling)
🤝 **Vandrevala Foundation**: 1860-2662-345 (24x7)"

## Rules
– Be warm and human – never robotic
– Hindi/English mix is fine
– Never give up on the user – always offer a clear next step
– Keep response concise – 3–5 sentences max before asking what they need

## Output Format
Return ONLY plain text response – no JSON, no code blocks. Just natural conversation.
`;
async function runFallback(openai, message, history, idaResult) {
    const recentHistory = history.slice(-4).map((m)=>({
            role: m.role,
            content: m.content
        }));
    try {
        const response = await openai.chat.completions.create({
            model: 'gpt-4o',
            messages: [
                {
                    role: 'system',
                    content: FALLBACK_SYSTEM_PROMPT
                },
                ...recentHistory,
                {
                    role: 'user',
                    content: message || 'I need help.'
                }
            ],
            temperature: 0.5,
            max_tokens: 500
        });
        const responseText = response.choices[0].message.content || "I'm here. What happened?";
        return {
            message: responseText,
            metadata: {
                agent: 'Fallback',
                priority: 'NORMAL'
            }
        };
    } catch (error) {
        console.error('[Fallback] Error:', error);
        return {
            message: "I'm here to help. What happened, and how much was lost? Or call **1930** (National Cyber Helpline, 24x7, free).",
            metadata: {
                agent: 'Fallback',
                priority: 'NORMAL'
            }
        };
    }
}
}),
"[project]/lib/agents/fileComplaint.ts [app-route] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "runFileComplaint",
    ()=>runFileComplaint
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$store$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/lib/store.ts [app-route] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$freezeDecision$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/lib/freezeDecision.ts [app-route] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$complaint$2d$requirements$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/lib/complaint-requirements.ts [app-route] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$uuid$2f$dist$2d$node$2f$v4$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__$3c$export__default__as__v4$3e$__ = __turbopack_context__.i("[project]/node_modules/uuid/dist-node/v4.js [app-route] (ecmascript) <export default as v4>");
;
;
;
;
const FILE_COMPLAINT_SYSTEM_PROMPT = `You are the File Complaint Agent of the Cyber Crime Reporting Portal.
Your mission: Help fraud victims file a cybercrime complaint as FAST as possible.

## GOLDEN RULE: DO NOT RE-ASK FOR DATA ALREADY EXTRACTED
If a field appears in "CURRENT EXTRACTED STATE" section with a value, DO NOT ASK FOR IT.
Period. Full stop. This is your most important rule.

## Mandatory Fields for Complete Filing
1. ✅ Transaction ID / UTR (usually extracted from screenshot)
2. ✅ Amount stolen (usually extracted from screenshot)
3. ✅ Destination VPA/account (usually extracted from screenshot)
4. ✅ Time of fraud (usually extracted from screenshot)
5. ⚠️ User's phone number (MUST collect if not extracted)
6. ✅ User's location (may be extracted)
7. ⚠️ Fraud narrative (collect before filing)
8. ⚠️ Identity Verification (MUST verify phone with OTP if not pre-authenticated)

## Workflow - ONE QUESTION AT A TIME

**Step 1: Show Golden Hour Alert (if < 2 hours)**
"⚡ GOLDEN HOUR ACTIVE: Act now - funds may still be recoverable!"

**Step 2: List What We Have**
Show extracted values clearly. Example:
"✓ I have: ₹8400 to xyz@upi, Transaction ID: 770941216078"

**Step 3: Ask ONLY for Missing Fields**
Priority: Phone > Location > Narrative > OTP Verification
- If phone missing: "What's your phone number?"
- If location missing: "What's your pincode or city?"
- If narrative missing: "What happened? (1-2 sentences)"
- If OTP needed: "I've sent an OTP to your number. Please enter it to verify."

**Step 4: Confirmation**
"Ready to file? Confirm to proceed."

**Step 5: File & Show CCN**

## OTP VERIFICATION FLOW (Only if user not pre-authenticated)
- When user provides phone number, mention: "I'll send a verification code to this number."
- Wait for user to confirm or provide OTP
- Guide them: "Enter the 4-6 digit code you received"
- Once OTP is verified, proceed with filing

## CRITICAL INSTRUCTIONS
- READ the "CURRENT EXTRACTED STATE" - it lists what's ALREADY COLLECTED
- READ the "AUTHENTICATION STATUS" - it tells if OTP verification is needed
- If field is there with value, SKIP IT - don't ask again
- If field shows "NOT YET PROVIDED", ask for it ONCE only - NEVER ask the same field twice
- Do NOT combine questions
- Do NOT ask for Transaction ID, Amount, VPA, or Time if they're already in the state
- Check conversation history - if you already asked "What's your phone number?" don't ask again
- Be warm but fast
- Never ask for the same info twice in same conversation window - track what's been asked

## Output Format
Plain text only. No JSON, no code blocks.
`;
async function runFileComplaint(openai, message, history, idaResult, route, userLocation, fileBase64, fileMediaType, previousExtracted, phoneFromSession// Phone number from OTP login session
) {
    // Merge previous extracted data with current (current takes precedence)
    let ext = {
        ...previousExtracted,
        ...idaResult.extracted
    };
    // Pre-fill phone from session if user logged in via OTP
    if (phoneFromSession && !ext.user_phone) {
        ext = {
            ...ext,
            user_phone: phoneFromSession
        };
        console.log(`[FileComplaint] Pre-filled phone from OTP session: ${phoneFromSession}`);
    }
    // Check if user is authenticated (has phone from session)
    const isUserAuthenticated = !!phoneFromSession;
    // Check if we have all mandatory fields to file
    const hasDestinationInfo = !!(ext.destination_vpa_or_account || ext.recipient_phone || ext.recipient_name);
    const canFile = !!(ext.utr_or_transaction_id && ext.amount_stolen && hasDestinationInfo && ext.time_since_fraud_minutes !== null && ext.user_phone && userLocation && isUserAuthenticated // Can only file if authenticated or OTP verified
    );
    // Generate CCN if filing
    const ccn = canFile ? `CCN-${new Date().getFullYear()}-${Math.floor(100000 + Math.random() * 900000)}` : null;
    // Format time nicely
    const formatTime = (minutes)=>{
        if (!minutes) return 'Unknown time';
        if (minutes < 60) return `${minutes} minutes ago`;
        if (minutes < 1440) return `${Math.floor(minutes / 60)} hour${Math.floor(minutes / 60) > 1 ? 's' : ''} ago`;
        if (minutes < 10080) return `${Math.floor(minutes / 1440)} day${Math.floor(minutes / 1440) > 1 ? 's' : ''} ago`;
        return `${Math.floor(minutes / 10080)} week${Math.floor(minutes / 10080) > 1 ? 's' : ''} ago`;
    };
    // Track which fields are missing (check conversation history to avoid re-asking)
    const missingFields = [];
    const recentConversation = history.slice(-10).map((m)=>m.content.toLowerCase()).join(' ');
    if (!ext.utr_or_transaction_id) missingFields.push('UTR_TRANSACTION_ID');
    if (!ext.amount_stolen) missingFields.push('AMOUNT');
    // Only ask for VPA_ACCOUNT if we don't have ANY destination info (VPA, phone, or name)
    if (!hasDestinationInfo) missingFields.push('VPA_ACCOUNT');
    if (!userLocation) missingFields.push('LOCATION');
    // Only ask for phone if not already provided AND not already asked in this conversation
    if (!ext.user_phone && !recentConversation.includes('phone number')) {
        missingFields.push('PHONE');
    }
    const fieldPriority = {
        UTR_TRANSACTION_ID: 1,
        AMOUNT: 2,
        VPA_ACCOUNT: 3,
        LOCATION: 4,
        PHONE: 5
    };
    const nextMissingField = missingFields.length > 0 ? missingFields.sort((a, b)=>(fieldPriority[a] || 99) - (fieldPriority[b] || 99))[0] : null;
    const missingFieldLabels = {
        UTR_TRANSACTION_ID: 'Transaction ID / UTR',
        AMOUNT: 'Amount (in rupees)',
        VPA_ACCOUNT: 'Destination VPA or Account',
        LOCATION: 'Location (pincode or city)',
        PHONE: 'Phone number (for verification)'
    };
    const missingFieldsList = missingFields.length === 0 ? '✓ ALL FIELDS COLLECTED - Ready to file' : `⚠️ Missing ${missingFields.length} field(s) in priority order:\n${missingFields.map((f, i)=>`   ${i + 1}. ${missingFieldLabels[f]}`).join('\n')}`;
    // Build the upfront "what you'll need" checklist for this fraud category so the
    // user sees everything required in one shot instead of turn-by-turn prompting.
    const isFirstFileComplaintTurn = !history.some((m)=>m.role === 'assistant');
    const checklist = (0, __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$complaint$2d$requirements$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["buildChecklist"])(ext.fraud_category, ext, !!fileBase64 || !!ext.utr_or_transaction_id // a screenshot was ever supplied this session
    );
    const nextTaskInstructions = missingFields.length === 0 ? 'TASK: Show summary of complaint details. Ask for confirmation: "Should I file your complaint with these details?" Do NOT ask for additional fields.' : `TASK: Ask for the FIRST missing field ONLY: ${missingFieldLabels[nextMissingField]}. Do NOT ask for multiple fields.`;
    // Build destination display for context
    const destinationDisplay = ext.destination_vpa_or_account ? ext.destination_vpa_or_account : [
        ext.recipient_name,
        ext.recipient_phone
    ].filter(Boolean).join(' | ') || null;
    const stateContext = `
## 🔒 AUTHENTICATION STATUS
${isUserAuthenticated ? `✅ User authenticated (pre-verified via OTP login)` : `⚠️ User NOT authenticated - OTP verification will be required before filing`}

## 🟢 EXTRACTED STATE (DO NOT ASK FOR THESE - ALREADY COLLECTED)
${ext.utr_or_transaction_id ? `✅ UTR/Transaction ID: ${ext.utr_or_transaction_id}` : `❌ Transaction ID: NOT PROVIDED`}
${ext.amount_stolen ? `✅ Amount: ₹${ext.amount_stolen}` : `❌ Amount: NOT PROVIDED`}
${destinationDisplay ? `✅ Destination/Recipient: ${destinationDisplay}` : `❌ Destination: NOT PROVIDED`}
${ext.payment_platform ? `✅ Platform: ${ext.payment_platform}` : `⚠️ Platform: Unknown`}
${ext.golden_hour_active ? `🔥 GOLDEN HOUR ACTIVE` : `⏳ Not urgent (>2 hours)`}
${userLocation ? `✅ Location: ${userLocation}` : `❌ Location: NOT PROVIDED`}

## 🔴 STILL NEED (ASK FOR THESE)
${ext.user_phone ? `✅ Phone: ${ext.user_phone}` : `❌ Phone: MUST COLLECT`}
${missingFieldsList}
${!isUserAuthenticated ? `\n⚠️ OTP Verification: Required after phone number is provided` : ''}

## USER JUST SAID
"${message}"

## WHAT TO DO NOW
${nextTaskInstructions}

GOLDEN RULE: If field shows ✅ above, DO NOT ASK FOR IT. Only ask for fields marked ❌ or from "STILL NEED" section.
IMPORTANT: If user provided phone number + name as recipient (number-to-number transfer), do NOT ask for VPA - that's complete destination info.
${!isUserAuthenticated ? `\nNOTE: User is not authenticated. After collecting phone number, guide them through OTP verification before filing.` : ''}
`;
    const recentHistory = history.slice(-6).map((m)=>({
            role: m.role,
            content: m.content
        }));
    const userContent = [
        {
            type: 'text',
            text: message || 'Please help me file the complaint.'
        }
    ];
    if (fileBase64 && fileMediaType?.startsWith('image/')) {
        userContent.push({
            type: 'image_url',
            image_url: {
                url: `data:${fileMediaType};base64,${fileBase64}`,
                detail: 'high'
            }
        });
    }
    // If we have extracted data from image, show summary first
    let summaryResponse = '';
    if (fileBase64 && fileMediaType?.startsWith('image/') && (ext.amount_stolen || ext.utr_or_transaction_id || ext.destination_vpa_or_account || ext.recipient_phone)) {
        const incidentTime = ext.time_since_fraud_minutes ? ext.time_since_fraud_minutes < 120 ? `${ext.time_since_fraud_minutes} minutes ago (⚡ GOLDEN HOUR ACTIVE)` : `${Math.floor(ext.time_since_fraud_minutes / 60)} hours ago` : 'Unknown';
        // Build recipient display from available info
        const recipientDisplay = ext.destination_vpa_or_account ? ext.destination_vpa_or_account : [
            ext.recipient_name,
            ext.recipient_phone
        ].filter(Boolean).join(' | ') || 'Not readable';
        summaryResponse = `
✅ **I've analyzed your screenshot. Here's what I found:**

📊 **Transaction Details:**
• **Amount:** ₹${ext.amount_stolen || 'Not readable'}
• **Sent to:** ${recipientDisplay}
• **Transaction ID:** ${ext.utr_or_transaction_id || ext.phonepe_transaction_id || 'Not readable'}
• **Platform:** ${ext.payment_platform || 'Not identified'}
• **Time of incident:** ${incidentTime}
${ext.golden_hour_active ? '🔥 **⚡ GOLDEN HOUR ACTIVE - Funds may still be recoverable!**' : ''}

Now I need a few more details to file your complaint:
`;
    }
    try {
        const response = await openai.chat.completions.create({
            model: 'gpt-4o',
            messages: [
                {
                    role: 'system',
                    content: FILE_COMPLAINT_SYSTEM_PROMPT + '\n\n' + stateContext
                },
                ...recentHistory,
                {
                    role: 'user',
                    content: userContent
                }
            ],
            temperature: 0.3,
            max_tokens: 800
        });
        const responseText = response.choices[0].message.content || 'Please try again.';
        // Prepend summary if image was processed
        let finalResponse = summaryResponse ? summaryResponse + responseText : responseText;
        // On the first turn we route into complaint filing, surface the full checklist
        // of what's needed upfront instead of asking for items one at a time.
        if (isFirstFileComplaintTurn) {
            finalResponse = finalResponse + '\n\n[[CHECKLIST]]';
        }
        // Check if user confirmed filing (look for affirmative responses)
        const userConfirmedFiling = /\b(yes|sure|go ahead|proceed|file|submit|confirm)\b/i.test(message);
        const isFilingComplaint = userConfirmedFiling && canFile && userLocation;
        // If filing, save complaint to in-memory store
        let savedComplaintId = '';
        let fileSuccessResponse = finalResponse;
        if (isFilingComplaint && canFile && userLocation) {
            const jurisdiction = (0, __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$store$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["getPincodeJurisdiction"])(userLocation);
            const complaintId = `complaint_${(0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$uuid$2f$dist$2d$node$2f$v4$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__$3c$export__default__as__v4$3e$__["v4"])()}`;
            try {
                // Build receiver info from available fields
                const receiverInfo = ext.destination_vpa_or_account ? ext.destination_vpa_or_account : [
                    ext.recipient_name,
                    ext.recipient_phone
                ].filter(Boolean).join(' | ') || 'Unknown';
                (0, __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$store$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["addComplaint"])({
                    id: complaintId,
                    ccn: ccn,
                    complainantPhone: ext.user_phone,
                    complainantName: '',
                    complainantLocation: userLocation,
                    fraudNarrative: message,
                    amount: parseInt(String(ext.amount_stolen).replace(/,/g, '')) || 0,
                    receiver: receiverInfo,
                    utr: ext.utr_or_transaction_id,
                    paymentPlatform: ext.payment_platform || 'Unknown',
                    timestamp: new Date(),
                    status: 'FILED',
                    confidenceScore: idaResult.confidence || 0.7,
                    assignedJurisdiction: jurisdiction,
                    frozenAccounts: [],
                    timeline: [
                        {
                            timestamp: new Date(),
                            action: 'Complaint Filed',
                            actor: 'SYSTEM',
                            details: `Auto-assigned to ${jurisdiction}`
                        }
                    ],
                    escalatedToAdmin: false
                });
                savedComplaintId = complaintId;
                // Decide on account freeze action
                const freezeDecision = (0, __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$freezeDecision$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["decideFreezeAction"])({
                    confidenceScore: idaResult.confidence || 0.7,
                    amountInRupees: parseInt(String(ext.amount_stolen).replace(/,/g, '')) || 0,
                    isGoldenHour: ext.golden_hour_active || false,
                    currentHour: new Date().getHours(),
                    fraudCategory: ext.fraud_category || 'UNKNOWN'
                });
                // Get today's date for complaint filing
                const today = new Date();
                const complaintDate = today.toLocaleDateString('en-IN', {
                    year: 'numeric',
                    month: 'long',
                    day: 'numeric'
                });
                // Calculate incident date from time_since_fraud_minutes
                const incidentDate = ext.time_since_fraud_minutes ? new Date(Date.now() - ext.time_since_fraud_minutes * 60000).toLocaleDateString('en-IN', {
                    year: 'numeric',
                    month: 'long',
                    day: 'numeric'
                }) : 'Date not specified';
                // Build destination display from available info
                const filedRecipientDisplay = ext.destination_vpa_or_account ? ext.destination_vpa_or_account : [
                    ext.recipient_name,
                    ext.recipient_phone
                ].filter(Boolean).join(' | ') || 'Unknown';
                // Build detailed success response with CCN and track status link
                fileSuccessResponse = `
✅ **Your complaint has been filed successfully!**

🔐 **Cyber Crime Number (CCN):** \`${ccn}\`

📋 **Complaint Summary:**
• **Incident Date:** ${incidentDate}
• **Complaint Filed:** ${complaintDate}
• **Amount:** ₹${ext.amount_stolen}
• **Destination:** ${filedRecipientDisplay}
• **Transaction ID:** ${ext.utr_or_transaction_id || ext.phonepe_transaction_id}
• **Platform:** ${ext.payment_platform || 'Unknown'}
• **Location:** ${userLocation}
• **Assigned to:** ${jurisdiction} Police Station

🔍 **Track Your Complaint Status:** [Click here to view your complaint status](/dashboard?tab=track-complaints)

**What happens next:**
1. Your complaint will be reviewed by local cyber police
2. You will receive updates via SMS/call on your registered number
3. Keep your CCN safe for future reference

**Stay alert:** Avoid further suspicious communications. If you receive calls demanding money, report them immediately by calling **1930** (24×7 Helpline).
`;
                console.log(`[FileComplaint] Complaint ${complaintId} filed with CCN ${ccn}. Freeze decision: ${freezeDecision.action}`);
            } catch (storeErr) {
                console.error('[FileComplaint] Failed to save complaint:', storeErr);
            }
        }
        // Detect if OTP verification is needed (user just provided phone number and not authenticated)
        const userJustProvidedPhone = ext.user_phone && !isUserAuthenticated && message.toLowerCase().match(/\d{10}/);
        const needsOTPVerification = userJustProvidedPhone;
        return {
            message: isFilingComplaint ? fileSuccessResponse : finalResponse,
            metadata: {
                agent: 'File Complaint',
                priority: route.priority,
                ccn: isFilingComplaint ? ccn : undefined,
                goldenHour: ext.golden_hour_active,
                complaintId: isFilingComplaint ? savedComplaintId : undefined,
                // OTP flow metadata
                requiresOTP: needsOTPVerification,
                otpPhone: needsOTPVerification ? ext.user_phone : undefined,
                userAuthenticated: isUserAuthenticated,
                // Upfront checklist of required info/evidence for this fraud category
                checklist: isFilingComplaint ? undefined : checklist,
                showChecklist: isFirstFileComplaintTurn && !isFilingComplaint,
                // Persist extracted state for next turn
                extractedState: {
                    utr_or_transaction_id: ext.utr_or_transaction_id,
                    phonepe_transaction_id: ext.phonepe_transaction_id,
                    amount_stolen: ext.amount_stolen,
                    destination_vpa_or_account: ext.destination_vpa_or_account,
                    recipient_name: ext.recipient_name,
                    recipient_phone: ext.recipient_phone,
                    payment_platform: ext.payment_platform,
                    time_since_fraud_minutes: ext.time_since_fraud_minutes,
                    user_phone: ext.user_phone,
                    user_location: userLocation,
                    golden_hour_active: ext.golden_hour_active
                }
            }
        };
    } catch (error) {
        console.error('[FileComplaint] Error:', error);
        return {
            message: 'Please try again, or call **1930** for immediate human assistance.',
            metadata: {
                agent: 'File Complaint',
                priority: 'NORMAL'
            }
        };
    }
}
}),
"[project]/lib/agents/ida.ts [app-route] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "runIDA",
    ()=>runIDA
]);
// Helper: Extract previously mentioned transaction details from history
function extractContextFromHistory(history) {
    const previousDetails = [];
    for (const entry of history){
        if (entry.role === 'assistant') {
            // Look for patterns like "₹X", "UTR/Transaction ID: ABC", "sent to X@bank", etc.
            const content = entry.content;
            if (content.includes('₹') || content.includes('sent to') || content.includes('Transaction ID') || content.includes('UTR')) {
                previousDetails.push(content);
            }
        }
    }
    if (previousDetails.length === 0) return '';
    return `\n## Previously Mentioned in Conversation:\n${previousDetails.join('\n')}\n(If the user is adding to or confirming these details, merge them.)`;
}
const IDA_SYSTEM_PROMPT = `You are the Intent Discovery Module (IDA) of the Cyber Crime Reporting Portal – India's AI-powered cyber fraud response system.
CRITICAL: You are the FIRST point of contact. You MUST process ALL images/files.

🔴 IF AN IMAGE IS PROVIDED IN THIS MESSAGE, YOU MUST EXTRACT ALL VISIBLE DATA FROM IT.
DO NOT SKIP IMAGE ANALYSIS. DO NOT SAY YOU CANNOT IDENTIFY DATA IF IT'S VISIBLE.
EVERY NUMBER, VPA, AMOUNT, DATE, NAME, PHONE IN THE IMAGE MUST BE EXTRACTED.

## Current Context (IMPORTANT FOR TIME CALCULATIONS)
Current date/time: ${new Date().toISOString()}

## Your Mission
1. Understand the user's intent (financial fraud or consumer dispute)
2. Extract structured parameters from unstructured input – CONVERSATIONALLY, never using forms
3. Classify intent into exactly one of: FILE_COMPLAINT | CHECK_STATUS | GENERAL_INFO | AMBIGUOUS
4. Identify fraud category: UPI_FRAUD | PHISHING | ECOMMERCE_SCAM | COMMERCIAL_DISPUTE | UNKNOWN

## Image/Screenshot Analysis (PhonePe, Google Pay, ICICI, etc.) - MANDATORY
When an image is provided - YOU MUST extract ALL fields visible:
– Transaction ID / UTR: Look for exact numbers like "770941216078" or text "UTR:", "Ref:", "Transaction ID:", "PhonePe ID:", etc.
– Amount: Extract number with rupee symbol like "₹8400" or amount field
– Recipient Details: Extract ANY of: VPA (name@upi), phone number (+91...), account name, or account number
– Timestamp: Parse date/time from screenshot (e.g., "05:04 pm on 23 Aug 2026") and calculate minutes since fraud
– Sender/Payer: Extract who sent the money (if visible)

CRITICAL RULES FOR TIMESTAMP EXTRACTION:
1. If screenshot shows date/time like "05:04 pm on 23 Aug 2026", parse it exactly
2. Calculate time_since_fraud_minutes by comparing screenshot datetime to current time
3. Do NOT return null for time - always calculate if any timestamp is visible

CRITICAL RULES FOR RECIPIENT EXTRACTION:
1. Extract recipient_name if visible (e.g., "Thejashwini V Gowda", "theju v gowda")
2. Extract recipient_phone if visible (e.g., "+916362139321")
3. Extract destination_vpa_or_account if it's a VPA like "name@upi" or account number
4. If ONLY a name+phone is visible (no VPA), that's still valid - use recipient_name and recipient_phone

CRITICAL RULES FOR ALL EXTRACTION:
1. If you can see the screenshot, EXTRACT ALL VISIBLE NUMBERS AND DATA
2. Do NOT say "I'm unable to identify" if data is visible - extract it
3. Respond with exact values from the screenshot
4. Only ask user to clarify if genuinely unreadable (blurry/cut off)
5. ALWAYS extract from image - this is your primary job

## Parameters to Extract (NEVER hallucinate – only extract what is actually present)
– utr_or_transaction_id: Transaction reference / UTR number (highest priority)
– phonepe_transaction_id: PhonePe-specific transaction ID (if different from UTR)
– amount_stolen: Amount in INR (NUMERIC ONLY, no commas or symbols: "1,97,000" becomes "197000")
– destination_vpa_or_account: Receiver VPA (e.g. scammer@upi) OR account number (optional if phone/name provided)
– recipient_name: Name of the person/entity who received the money
– recipient_phone: Phone number of the recipient (format: +91...)
– payment_platform: GPay | PhonePe | Paytm | NEFT | IMPS | RTGS | Net Banking | Other
– time_since_fraud_minutes: Minutes since fraud occurred (CALCULATE from visible timestamp and current time)
– time_display: Human-friendly format: "2 hours ago" | "1 day ago" | "3 weeks ago" (based on minutes)
– user_phone: User's registered mobile number
– user_location: User's pincode or city (only from what the user has explicitly stated)
– golden_hour_active: true if time_since_fraud_minutes < 120, false otherwise

## Important: Reuse Previously Extracted Data
If the conversation history shows that a field was already extracted in a previous turn (e.g., "I can see ₹X was sent to Y@bank"), reuse that value unless the current message contradicts it. This ensures consistency across the multi-turn conversation.

CRITICAL: Handle Indian number format. If user says "1,97,000" or "₹1,97,000 rupees", extract amount_stolen as "197000" (numeric only).

## Tone & Language Rules
– Warm, empathetic, calming – victims are panicked and distressed
– Hindi/English/Hinglish mix is fully acceptable
– NEVER ask more than ONE clarifying question per message
– If fraud happened < 2 hours ago: show URGENCY (use "⚡" emoji, emphasize speed)
– Never make the user feel blamed or ashamed

## Output Format (MUST return valid JSON)
IMPORTANT: Return ONLY valid JSON object. Do not add any text before or after JSON.
{
  "conversationalReply": "Your full, warm, natural language response to the user",
  "extracted": {
    "intent": "FILE_COMPLAINT|CHECK_STATUS|GENERAL_INFO|AMBIGUOUS",
    "fraud_category": "UPI_FRAUD|PHISHING|ECOMMERCE_SCAM|COMMERCIAL_DISPUTE|UNKNOWN",
    "utr_or_transaction_id": null,
    "phonepe_transaction_id": null,
    "amount_stolen": null,
    "destination_vpa_or_account": null,
    "recipient_name": null,
    "recipient_phone": null,
    "payment_platform": null,
    "time_since_fraud_minutes": null,
    "user_phone": null,
    "user_location": null,
    "golden_hour_active": false
  },
  "confidence": 0.85,
  "missing_critical_fields": ["utr_or_transaction_id", "amount_stolen"]
}
`;
async function runIDA(openai, message, history, fileBase64, fileMediaType, accumulatedExtracted) {
    const recentHistory = history.slice(-8).map((m)=>({
            role: m.role,
            content: m.content
        }));
    // Extract any transaction details already mentioned in conversation
    // This helps reuse previously extracted data across turns
    const previouslyExtractedContext = extractContextFromHistory(history);
    // Add accumulated extracted data to context
    const accumulatedContext = accumulatedExtracted ? `
## PREVIOUSLY EXTRACTED (Do not override unless user explicitly corrects):
- UTR: ${accumulatedExtracted.utr_or_transaction_id || 'Not yet extracted'}
- Amount: ${accumulatedExtracted.amount_stolen || 'Not yet extracted'}
- VPA/Account: ${accumulatedExtracted.destination_vpa_or_account || 'Not yet extracted'}
- Location: ${accumulatedExtracted.user_location || 'Not yet extracted'}
- Time: ${accumulatedExtracted.time_since_fraud_minutes || 'Not yet calculated'}
- Platform: ${accumulatedExtracted.payment_platform || 'Not yet identified'}

If user provides NEW information, extract and update. Otherwise, PRESERVE previous values.
` : '';
    // Build user content – PRIORITY: Image first, then text
    const userContent = [];
    // ALWAYS process image if present - this is critical
    if (fileBase64 && fileMediaType?.startsWith('image/')) {
        console.log(`[IDA] 🖼️ PROCESSING IMAGE: ${fileMediaType}, Size: ${fileBase64.length} chars`);
        userContent.push({
            type: 'image_url',
            image_url: {
                url: `data:${fileMediaType};base64,${fileBase64}`,
                detail: 'high'
            }
        });
    } else if (fileBase64 && fileMediaType === 'application/pdf') {
        console.log('[IDA] Processing PDF');
        userContent.push({
            type: 'text',
            text: '[PDF document attached – please extract any visible transaction details]'
        });
    }
    // Add text message (unless it's just the default upload message)
    if (message && !message.includes('I have uploaded a file for analysis')) {
        userContent.push({
            type: 'text',
            text: message
        });
    } else if (!fileBase64 && message) {
        // If no file, always include message
        userContent.push({
            type: 'text',
            text: message
        });
    }
    try {
        const systemPrompt = IDA_SYSTEM_PROMPT + previouslyExtractedContext + accumulatedContext;
        const response = await openai.chat.completions.create({
            model: 'gpt-4o',
            messages: [
                {
                    role: 'system',
                    content: systemPrompt
                },
                ...recentHistory,
                {
                    role: 'user',
                    content: userContent
                }
            ],
            response_format: {
                type: 'json_object'
            },
            temperature: 0.2,
            max_tokens: 1000
        });
        const raw = response.choices[0].message.content || '{}';
        const parsed = JSON.parse(raw);
        // Sanitize amount: remove commas and convert to string number
        if (parsed.extracted?.amount_stolen) {
            const sanitized = String(parsed.extracted.amount_stolen).replace(/,/g, '').trim();
            parsed.extracted.amount_stolen = sanitized;
        }
        // LOG EXTRACTED DATA
        if (parsed.extracted) {
            console.log(`
📊 [IDA EXTRACTION RESULT]
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
✅ Intent: ${parsed.extracted.intent}
💳 Transaction ID: ${parsed.extracted.utr_or_transaction_id || 'NOT FOUND'}
💰 Amount: ₹${parsed.extracted.amount_stolen || 'NOT FOUND'}
🏦 Destination: ${parsed.extracted.destination_vpa_or_account || 'NOT FOUND'}
📱 Platform: ${parsed.extracted.payment_platform || 'NOT FOUND'}
⏰ Time since fraud: ${parsed.extracted.time_since_fraud_minutes} minutes
🔥 Golden Hour Active: ${parsed.extracted.golden_hour_active ? 'YES' : 'NO'}
📈 Confidence: ${parsed.confidence || 'N/A'}
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
      `);
        }
        return parsed;
    } catch (err) {
        console.error('[IDA] Parse error:', err);
        return {
            conversationalReply: "I'm here to help you. Could you tell me – what happened and how much was lost?",
            extracted: {
                intent: 'AMBIGUOUS',
                fraud_category: 'UNKNOWN',
                golden_hour_active: false
            },
            confidence: 0.1,
            missing_critical_fields: [
                'intent',
                'fraud_category'
            ]
        };
    }
}
}),
"[project]/lib/agents/retrieval.ts [app-route] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "runRetrieval",
    ()=>runRetrieval
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$store$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/lib/store.ts [app-route] (ecmascript)");
;
const STATUS_LABELS = {
    FILED: '📋 Filed',
    PENDING_CLARIFICATION: '⏳ Pending Clarification',
    UNDER_INVESTIGATION: '🔍 Under Investigation',
    ACCOUNT_FROZEN: '💰 Funds Frozen',
    RESOLVED: '✅ Resolved'
};
function maskPhone(phone) {
    return phone.length >= 4 ? `XXXXXX${phone.slice(-4)}` : phone;
}
function formatComplaintList(complaints) {
    return complaints.map((c, i)=>{
        const date = new Date(c.timestamp).toLocaleDateString('en-IN', {
            year: 'numeric',
            month: 'short',
            day: 'numeric'
        });
        return `${i + 1}️⃣ ☑️ [${c.ccn}] | ₹${c.amount.toLocaleString('en-IN')} | ${c.paymentPlatform} | Filed: ${date} | Status: ${STATUS_LABELS[c.status]}`;
    }).join('\n');
}
function formatComplaintDetail(c) {
    const date = new Date(c.timestamp).toLocaleDateString('en-IN', {
        year: 'numeric',
        month: 'long',
        day: 'numeric',
        hour: '2-digit',
        minute: '2-digit'
    });
    return `
🔐 CCN: ${c.ccn}
💰 Amount: ₹${c.amount.toLocaleString('en-IN')}
🏦 Sent to: ${c.receiver}
💳 Transaction ID: ${c.utr}
📱 Platform: ${c.paymentPlatform}
📅 Filed on: ${date}
📍 Jurisdiction: ${c.assignedJurisdiction} Police Station
📊 Status: ${STATUS_LABELS[c.status]}
${c.frozenAccounts.length > 0 ? `🧊 Frozen Accounts: ${c.frozenAccounts.join(', ')}` : ''}
`.trim();
}
const RETRIEVAL_SYSTEM_PROMPT = (phoneFromSession, complaintsContext)=>`You are the Retrieval Agent of the Cyber Crime Reporting Portal.
Your job: Help users check status of their previously filed complaints – securely and clearly.

${phoneFromSession ? `## USER STATUS
✅ You are logged in with phone: +91${phoneFromSession}
Skip Step 1-2 - go directly to Step 3 to retrieve complaints for this phone number.` : ''}

## Workflow
Step 1: ${phoneFromSession ? '(SKIPPED - User authenticated)' : 'Ask for registered phone number (if not provided).'}
Step 2: ${phoneFromSession ? '(SKIPPED - User authenticated)' : 'Trigger OTP verification: "I have sent an OTP to XXXXXX[last 2 digits]. Please enter the 4-6 digit code to verify."'}
Step 3: Accept any 4-6 digit number as a valid OTP (simulated for hackathon).
Step 4: Present the complaints listed below EXACTLY as given – do NOT invent, alter, or add any complaints, amounts, statuses, or CCNs that are not in this list.

${complaintsContext || '## REAL COMPLAINT DATA\nNo complaint lookup has been performed yet, or none was found for this phone number. If asked to list complaints, say none were found and offer to file a new one.'}

Then ask: "Please select the complaint you want to view by entering its NUMBER (1, 2, 3, etc.)"

## Rules
– CRITICAL: Only use the REAL COMPLAINT DATA provided above. NEVER fabricate CCNs, amounts, dates, or statuses.
– Mask sensitive data: show only last 4 digits of account numbers, phone as XXXXXX78
– When listing complaints: Use numbers (1, 2, 3, 4) – make this CRYSTAL CLEAR in your prompt
– Do NOT ask for CCN number when selecting complaints – ask for the list NUMBER (1-4)
– If "no complaints found" – offer to file a new one, do not invent any

## Tone
Warm, reassuring. Victims are worried. Never stress them out with delays.

## Output Format
Return ONLY plain text response – no JSON, no code blocks. Just natural conversation.
`;
async function runRetrieval(openai, message, history, idaResult, phoneFromSession// Phone number from OTP login session
) {
    const recentHistory = history.slice(-8).map((m)=>({
            role: m.role,
            content: m.content
        }));
    // Only look up real complaints once we know the user's phone (session, or
    // extracted from an OTP-verified message earlier in this conversation).
    const lookupPhone = phoneFromSession || idaResult.extracted?.user_phone || null;
    let complaintsContext;
    let complaints = [];
    if (lookupPhone) {
        complaints = (0, __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$store$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["getComplaintsByPhone"])(lookupPhone);
        complaintsContext = complaints.length > 0 ? `## REAL COMPLAINT DATA (phone ${maskPhone(lookupPhone)}) – present these EXACTLY, do not modify:\n${formatComplaintList(complaints)}\n\n## FULL DETAILS (use when user selects a numbered complaint):\n${complaints.map((c, i)=>`--- Complaint ${i + 1} ---\n${formatComplaintDetail(c)}`).join('\n\n')}` : `## REAL COMPLAINT DATA (phone ${maskPhone(lookupPhone)})\nNo complaints found for this phone number. Tell the user clearly and offer to help file a new one.`;
    }
    try {
        const response = await openai.chat.completions.create({
            model: 'gpt-4o',
            messages: [
                {
                    role: 'system',
                    content: RETRIEVAL_SYSTEM_PROMPT(phoneFromSession || undefined, complaintsContext)
                },
                ...recentHistory,
                {
                    role: 'user',
                    content: message
                }
            ],
            temperature: 0.4,
            max_tokens: 700
        });
        const responseText = response.choices[0].message.content || 'Please try again.';
        return {
            message: responseText,
            metadata: {
                agent: 'Retrieval',
                priority: 'NORMAL',
                complaintsFound: complaints.length
            }
        };
    } catch (error) {
        console.error('[Retrieval] Error:', error);
        return {
            message: 'Please try again, or call **1930** for immediate human assistance.',
            metadata: {
                agent: 'Retrieval',
                priority: 'NORMAL'
            }
        };
    }
}
}),
"[project]/lib/agents/router.ts [app-route] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "runRouter",
    ()=>runRouter
]);
async function runRouter(idaResult) {
    const { extracted, confidence, missing_critical_fields } = idaResult;
    if (!extracted) {
        return {
            route_to: 'FALLBACK_AGENT',
            priority: 'NORMAL',
            reason: 'No extraction result from IDA',
            context_passed: idaResult.extracted || {}
        };
    }
    const { intent, fraud_category, golden_hour_active, utr_or_transaction_id, amount_stolen, destination_vpa_or_account } = extracted;
    const missingCount = (missing_critical_fields || []).length;
    // Check if we have mandatory fields (regardless of intent classification)
    const hasMandatoryFields = !!(utr_or_transaction_id && amount_stolen && destination_vpa_or_account);
    // If fraud detected with all mandatory fields → go to FILE_COMPLAINT
    if ((intent === 'FILE_COMPLAINT' || intent === 'AMBIGUOUS' && hasMandatoryFields) && hasMandatoryFields) {
        const isConsumerCase = [
            'ECOMMERCE_SCAM',
            'COMMERCIAL_DISPUTE'
        ].includes(fraud_category);
        return {
            route_to: 'FILE_COMPLAINT_AGENT',
            priority: golden_hour_active && !isConsumerCase ? 'URGENT' : 'NORMAL',
            reason: `${fraud_category} – all mandatory fields present, ready to file`,
            context_passed: extracted
        };
    }
    // Not enough info yet – keep gathering (but only if ambiguous/low confidence)
    if (confidence < 0.55 && missingCount > 1 && !hasMandatoryFields) {
        return {
            route_to: 'IDA',
            priority: 'NORMAL',
            reason: 'Insufficient confidence – collecting more context',
            context_passed: extracted
        };
    }
    // File Complaint routing (explicit intent)
    if (intent === 'FILE_COMPLAINT') {
        const isConsumerCase = [
            'ECOMMERCE_SCAM',
            'COMMERCIAL_DISPUTE'
        ].includes(fraud_category);
        return {
            route_to: 'FILE_COMPLAINT_AGENT',
            priority: golden_hour_active && !isConsumerCase ? 'URGENT' : 'NORMAL',
            reason: `${fraud_category} – complaint filing requested`,
            context_passed: extracted
        };
    }
    // Check Status routing
    if (intent === 'CHECK_STATUS') {
        return {
            route_to: 'RETRIEVAL_AGENT',
            priority: 'NORMAL',
            reason: 'User wants to check complaint status',
            context_passed: extracted
        };
    }
    // General info – fallback handles it
    if (intent === 'GENERAL_INFO' && confidence >= 0.6) {
        return {
            route_to: 'FALLBACK_AGENT',
            priority: 'NORMAL',
            reason: 'General information query',
            context_passed: extracted
        };
    }
    // Low confidence or ambiguous – fallback (warm conversation)
    return {
        route_to: 'FALLBACK_AGENT',
        priority: 'NORMAL',
        reason: 'Low confidence or ambiguous intent',
        context_passed: extracted
    };
}
}),
"[project]/lib/complaint-requirements.ts [app-route] (ecmascript)", ((__turbopack_context__) => {
"use strict";

// Per-fraud-category checklist of information/evidence needed to file a complaint.
// Surfaced upfront in chat so the user knows everything they'll need before going
// back and forth turn-by-turn.
__turbopack_context__.s([
    "CHECKLISTS",
    ()=>CHECKLISTS,
    "buildChecklist",
    ()=>buildChecklist
]);
const CHECKLISTS = {
    UPI_FRAUD: [
        {
            id: 'txn_screenshot',
            label: 'Screenshot or PDF of the transaction',
            description: 'Payment app screenshot or bank statement showing the transaction',
            satisfiedBy: [
                'file'
            ]
        },
        {
            id: 'utr',
            label: 'Transaction ID / UTR number',
            description: 'Usually visible on the payment screenshot',
            satisfiedBy: [
                'utr_or_transaction_id',
                'phonepe_transaction_id'
            ]
        },
        {
            id: 'amount',
            label: 'Amount lost',
            description: 'How much money was sent',
            satisfiedBy: [
                'amount_stolen'
            ]
        },
        {
            id: 'destination',
            label: "Recipient's UPI ID, account, or phone number",
            description: 'Where the money was sent',
            satisfiedBy: [
                'destination_vpa_or_account',
                'recipient_phone',
                'recipient_name'
            ]
        },
        {
            id: 'phone',
            label: 'Your phone number',
            description: 'For verification and updates on your complaint',
            satisfiedBy: [
                'user_phone'
            ]
        },
        {
            id: 'location',
            label: 'Your location (pincode or city)',
            description: 'To assign the right jurisdiction/police station',
            satisfiedBy: [
                'user_location'
            ]
        }
    ],
    PHISHING: [
        {
            id: 'evidence',
            label: 'Screenshot of the phishing message/email/site',
            description: 'The SMS, email, or fake website that tricked you',
            satisfiedBy: [
                'file'
            ]
        },
        {
            id: 'amount',
            label: 'Amount lost (if any money was taken)',
            description: 'How much money was lost, if applicable',
            satisfiedBy: [
                'amount_stolen'
            ]
        },
        {
            id: 'destination',
            label: "Recipient's UPI ID, account, or phone number (if applicable)",
            description: 'Where the money was sent, if any',
            satisfiedBy: [
                'destination_vpa_or_account',
                'recipient_phone',
                'recipient_name'
            ]
        },
        {
            id: 'phone',
            label: 'Your phone number',
            description: 'For verification and updates on your complaint',
            satisfiedBy: [
                'user_phone'
            ]
        },
        {
            id: 'location',
            label: 'Your location (pincode or city)',
            description: 'To assign the right jurisdiction/police station',
            satisfiedBy: [
                'user_location'
            ]
        }
    ],
    ECOMMERCE_SCAM: [
        {
            id: 'order_screenshot',
            label: 'Screenshot or PDF of the order/payment receipt',
            description: 'Order confirmation, invoice, or payment proof',
            satisfiedBy: [
                'file'
            ]
        },
        {
            id: 'amount',
            label: 'Amount paid',
            description: 'How much money was paid for the order',
            satisfiedBy: [
                'amount_stolen'
            ]
        },
        {
            id: 'seller',
            label: 'Seller/website/app name and contact details',
            description: 'Who you paid or ordered from',
            satisfiedBy: [
                'destination_vpa_or_account',
                'recipient_phone',
                'recipient_name'
            ]
        },
        {
            id: 'phone',
            label: 'Your phone number',
            description: 'For verification and updates on your complaint',
            satisfiedBy: [
                'user_phone'
            ]
        },
        {
            id: 'location',
            label: 'Your location (pincode or city)',
            description: 'To assign the right jurisdiction/police station',
            satisfiedBy: [
                'user_location'
            ]
        }
    ],
    COMMERCIAL_DISPUTE: [
        {
            id: 'agreement',
            label: 'Screenshot/PDF of invoice, agreement, or payment proof',
            description: 'Documentation of the transaction or agreement in dispute',
            satisfiedBy: [
                'file'
            ]
        },
        {
            id: 'amount',
            label: 'Amount involved',
            description: 'How much money is in dispute',
            satisfiedBy: [
                'amount_stolen'
            ]
        },
        {
            id: 'counterparty',
            label: 'Other party\'s name/business and contact details',
            description: 'Who you transacted with',
            satisfiedBy: [
                'destination_vpa_or_account',
                'recipient_phone',
                'recipient_name'
            ]
        },
        {
            id: 'phone',
            label: 'Your phone number',
            description: 'For verification and updates on your complaint',
            satisfiedBy: [
                'user_phone'
            ]
        },
        {
            id: 'location',
            label: 'Your location (pincode or city)',
            description: 'To assign the right jurisdiction/police station',
            satisfiedBy: [
                'user_location'
            ]
        }
    ],
    UNKNOWN: [
        {
            id: 'evidence',
            label: 'Any screenshot or PDF related to the incident',
            description: 'Payment proof, message, or receipt',
            satisfiedBy: [
                'file'
            ]
        },
        {
            id: 'amount',
            label: 'Amount lost (if any)',
            description: 'How much money was lost, if applicable',
            satisfiedBy: [
                'amount_stolen'
            ]
        },
        {
            id: 'phone',
            label: 'Your phone number',
            description: 'For verification and updates on your complaint',
            satisfiedBy: [
                'user_phone'
            ]
        },
        {
            id: 'location',
            label: 'Your location (pincode or city)',
            description: 'To assign the right jurisdiction/police station',
            satisfiedBy: [
                'user_location'
            ]
        }
    ]
};
function buildChecklist(fraudCategory, extracted, hasFile) {
    const category = CHECKLISTS[fraudCategory] ? fraudCategory : 'UNKNOWN';
    const items = CHECKLISTS[category];
    return items.map((item)=>({
            ...item,
            satisfied: item.satisfiedBy.some((field)=>field === 'file' ? hasFile : !!extracted[field])
        }));
}
}),
"[project]/lib/freezeDecision.ts [app-route] (ecmascript)", ((__turbopack_context__) => {
"use strict";

// Account freezing decision logic
// Input: complaint details
// Output: action (INSTANT_FREEZE, ESCALATE_TO_POLICE, MANUAL_REVIEW)
__turbopack_context__.s([
    "checkEscalation",
    ()=>checkEscalation,
    "decideFreezeAction",
    ()=>decideFreezeAction
]);
function decideFreezeAction(input) {
    const { confidenceScore, amountInRupees, isGoldenHour, currentHour, fraudCategory } = input;
    // High confidence + golden hour + significant amount → INSTANT FREEZE
    if (confidenceScore >= 0.8 && isGoldenHour && amountInRupees > 10000) {
        return {
            action: 'INSTANT_FREEZE',
            reason: `High confidence fraud (${(confidenceScore * 100).toFixed(0)}%) in Golden Hour with ₹${amountInRupees} loss. Immediate action required.`,
            frozenAccounts: [],
            urgency: 'CRITICAL',
            estimatedTimeToAction: 'Immediate (< 1 minute)'
        };
    }
    // Off-hours (night 22:00-06:00) + high confidence → INTELLIGENT AUTO-FREEZE
    const isNightTime = currentHour >= 22 || currentHour < 6;
    if (isNightTime && confidenceScore >= 0.75 && amountInRupees > 5000) {
        return {
            action: 'INSTANT_FREEZE',
            reason: `Off-hours fraud alert (${currentHour}:00). High confidence (${(confidenceScore * 100).toFixed(0)}%) detected during night time. Auto-freezing to prevent further loss.`,
            frozenAccounts: [],
            urgency: 'CRITICAL',
            estimatedTimeToAction: 'Immediate (< 30 seconds)'
        };
    }
    // Medium confidence + golden hour → ESCALATE to assigned police
    if (confidenceScore >= 0.6 && isGoldenHour && amountInRupees > 5000) {
        return {
            action: 'ESCALATE_TO_POLICE',
            reason: `Medium-high confidence (${(confidenceScore * 100).toFixed(0)}%) in Golden Hour. Assigned police to decide on account freeze.`,
            frozenAccounts: [],
            urgency: 'HIGH',
            estimatedTimeToAction: '5-10 minutes (pending police approval)'
        };
    }
    // High amount but lower confidence → MANUAL REVIEW
    if (confidenceScore < 0.6 && amountInRupees > 20000) {
        return {
            action: 'MANUAL_REVIEW',
            reason: `High amount (₹${amountInRupees}) but moderate confidence (${(confidenceScore * 100).toFixed(0)}%). Requires human verification before freeze.`,
            frozenAccounts: [],
            urgency: 'HIGH',
            estimatedTimeToAction: '15-30 minutes (admin review)'
        };
    }
    // Low confidence, lower amount → MANUAL REVIEW
    if (confidenceScore < 0.55) {
        return {
            action: 'MANUAL_REVIEW',
            reason: `Lower confidence score (${(confidenceScore * 100).toFixed(0)}%). Complaint filed but requires clarification from complainant.`,
            frozenAccounts: [],
            urgency: 'NORMAL',
            estimatedTimeToAction: '30-60 minutes (standard review)'
        };
    }
    // Medium confidence, reasonable amount → ESCALATE
    return {
        action: 'ESCALATE_TO_POLICE',
        reason: `Standard ${fraudCategory} case with confidence ${(confidenceScore * 100).toFixed(0)}%. Police to assess and freeze accounts.`,
        frozenAccounts: [],
        urgency: 'NORMAL',
        estimatedTimeToAction: '10-30 minutes (police decision)'
    };
}
function checkEscalation(status, filedAt, assignedPoliceId, amountInRupees) {
    const nowMs = Date.now();
    const filedAtMs = filedAt.getTime();
    const elapsedMinutes = (nowMs - filedAtMs) / (1000 * 60);
    // If pending clarification and > 10 min → escalate to admin
    if (status === 'PENDING_CLARIFICATION' && elapsedMinutes > 10) {
        return {
            shouldEscalate: true,
            escalateTo: 'ADMIN',
            reason: 'Complaint pending clarification for >10 minutes. Escalating to admin for follow-up.'
        };
    }
    // If under investigation and > 30 min (high amount) → escalate
    if (status === 'UNDER_INVESTIGATION' && amountInRupees && amountInRupees > 50000 && elapsedMinutes > 30) {
        return {
            shouldEscalate: true,
            escalateTo: 'ADMIN',
            reason: 'High-value complaint (₹' + amountInRupees + ') under investigation for >30 min. Escalating to admin.'
        };
    }
    // If assigned to police and no response for > 1 hour → escalate to admin
    if (assignedPoliceId && status === 'UNDER_INVESTIGATION' && elapsedMinutes > 60) {
        return {
            shouldEscalate: true,
            escalateTo: 'ADMIN',
            reason: 'Assigned police no response for >1 hour. Escalating to city admin.'
        };
    }
    return {
        shouldEscalate: false,
        escalateTo: '',
        reason: ''
    };
}
}),
"[project]/lib/store.ts [app-route] (ecmascript)", ((__turbopack_context__) => {
"use strict";

// In-memory store for demo (resets on server restart)
// Production: replace with database
__turbopack_context__.s([
    "addComplaint",
    ()=>addComplaint,
    "createSession",
    ()=>createSession,
    "deleteSession",
    ()=>deleteSession,
    "findUserByUsername",
    ()=>findUserByUsername,
    "getAllComplaints",
    ()=>getAllComplaints,
    "getComplaint",
    ()=>getComplaint,
    "getComplaintsByJurisdiction",
    ()=>getComplaintsByJurisdiction,
    "getComplaintsByPhone",
    ()=>getComplaintsByPhone,
    "getPincodeJurisdiction",
    ()=>getPincodeJurisdiction,
    "getSession",
    ()=>getSession,
    "getStore",
    ()=>getStore,
    "updateComplaint",
    ()=>updateComplaint
]);
// Global store
const store = {
    complaints: new Map(),
    users: new Map(),
    sessions: new Map()
};
// Demo accounts
const demoUsers = [
    {
        id: 'user_complainant_1',
        username: 'victim@example.com',
        password: 'password123',
        role: 'COMPLAINANT',
        phone: '9876543210',
        name: 'Priya Sharma'
    },
    {
        id: 'user_police_1',
        username: 'police@bangalore.gov',
        password: 'police123',
        role: 'POLICE',
        jurisdiction: 'Bangalore East',
        phone: '9876543200',
        name: 'Inspector Rajesh Kumar'
    },
    {
        id: 'user_police_2',
        username: 'police@bangalore_west.gov',
        password: 'police123',
        role: 'POLICE',
        jurisdiction: 'Bangalore West',
        phone: '9876543201',
        name: 'Inspector Anjali Singh'
    },
    {
        id: 'user_admin_bangalore',
        username: 'admin@bangalore.gov',
        password: 'admin123',
        role: 'ADMIN',
        jurisdiction: 'Bangalore',
        phone: '9876543202',
        name: 'Cyber Crime Head - Bangalore'
    },
    {
        id: 'user_admin_karnataka',
        username: 'admin@karnataka.gov',
        password: 'admin123',
        role: 'ADMIN',
        jurisdiction: 'Karnataka',
        phone: '9876543203',
        name: 'State Cyber Admin - Karnataka'
    }
];
// Initialize demo users
demoUsers.forEach((user)=>{
    store.users.set(user.id, user);
});
function getStore() {
    return store;
}
function addComplaint(complaint) {
    store.complaints.set(complaint.id, complaint);
    return complaint;
}
function getComplaint(id) {
    return store.complaints.get(id);
}
function getComplaintsByPhone(phone) {
    return Array.from(store.complaints.values()).filter((c)=>c.complainantPhone === phone);
}
function getComplaintsByJurisdiction(jurisdiction) {
    return Array.from(store.complaints.values()).filter((c)=>c.assignedJurisdiction === jurisdiction || c.assignedJurisdiction.startsWith(jurisdiction));
}
function getAllComplaints() {
    return Array.from(store.complaints.values());
}
function updateComplaint(id, updates) {
    const complaint = store.complaints.get(id);
    if (!complaint) return null;
    Object.assign(complaint, updates);
    return complaint;
}
function createSession(userId) {
    const user = store.users.get(userId);
    if (!user) return '';
    const sessionId = `session_${Date.now()}_${Math.random().toString(36).slice(2)}`;
    const session = {
        userId,
        username: user.username,
        role: user.role,
        jurisdiction: user.jurisdiction,
        expiresAt: new Date(Date.now() + 24 * 60 * 60 * 1000)
    };
    store.sessions.set(sessionId, session);
    return sessionId;
}
function getSession(sessionId) {
    const session = store.sessions.get(sessionId);
    if (session && session.expiresAt > new Date()) {
        return session;
    }
    store.sessions.delete(sessionId);
    return undefined;
}
function deleteSession(sessionId) {
    store.sessions.delete(sessionId);
}
function findUserByUsername(username) {
    return Array.from(store.users.values()).find((u)=>u.username === username);
}
function getPincodeJurisdiction(pincode) {
    // Hardcoded pincode → jurisdiction mapping for demo
    const map = {
        '560001': 'Bangalore East',
        '560002': 'Bangalore East',
        '560003': 'Bangalore East',
        '560004': 'Bangalore West',
        '560005': 'Bangalore West',
        '560006': 'Bangalore West',
        '560007': 'Bangalore North',
        '560008': 'Bangalore North',
        '560009': 'Bangalore North',
        '560010': 'Bangalore South'
    };
    return map[pincode] || 'Bangalore East' // default fallback
    ;
}
}),
];

//# sourceMappingURL=%5Broot-of-the-server%5D__1tnbl_p._.js.map